import Foundation

/// Names shared by the host registration and the presentation of calls from that host.
public enum ToolHostIdentity {
    public static let bridgeServerName = "bloom-workspace-bridge"
    public static let crewNameLimit = 32
}
