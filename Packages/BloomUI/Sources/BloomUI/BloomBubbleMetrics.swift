import SwiftUI

/// The outgoing message is the same object on every Apple platform.
public enum BloomBubbleMetrics {
    public static let maximumWidth: CGFloat = 560
    public static let leadingSpace: CGFloat = 32
    public static let corner: CGFloat = 12
    public static let padding = EdgeInsets(top: 8, leading: 12, bottom: 8, trailing: 12)
}
